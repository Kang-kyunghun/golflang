enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

enum Provider {
  LOCAL = 'local',
  GOOGLE = 'google',
  KAKAO = 'kakao',
}

export { Role, Gender, Provider };
